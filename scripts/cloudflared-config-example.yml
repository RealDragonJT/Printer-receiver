# Example Cloudflared named tunnel config
# Place in ~/.cloudflared/config.yml (macOS/Linux) or %USERPROFILE%\.cloudflared\config.yml (Windows)

tunnel: <your-tunnel-uuid>
credentials-file: <path-to-credentials-file>

ingress:
  - hostname: printer.yourdomain.com
    service: http://localhost:5000
  - service: http_status:404



